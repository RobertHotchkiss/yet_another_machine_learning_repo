[package]
name = "live_range_bars_rs"
version = "0.1.0"
edition = "2024"

[lib]
name = "live_range_bars_rs"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.27", features = ["extension-module"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

